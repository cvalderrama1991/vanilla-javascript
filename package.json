{
  "name": "vanilla-javascript",
  "version": "1.0.0",
  "type": "module",
  "description": "JavaScript application to test JavaScript without a framework",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch --env-file=.env server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Carlos",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.1"
  }
}
